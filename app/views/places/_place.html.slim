.col-md
  .card.text-white.card-has-bg style=("background-image: url(#{place.pictures[0].image.thumb.url})")
    .card-img-overlay.d-flex.flex-column
      .rating
        |⭐️4.9
      .card-footer
        .row
          .col-6
            .row
              .col-12
                h5
                  = place.title.capitalize
                .col-12
                  p = full_address(place.address)

          .col-6
            = link_to "places/#{place.id}"
              button.btn.btn-success type="button"
                = place.price
                | $
