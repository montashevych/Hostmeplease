setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
    worker: Dockerfile
  config:
    RAILS_ENV: production
    FOO: bar
release:
  image: worker
run:
  web: bundle exec puma -C config/puma.rb
